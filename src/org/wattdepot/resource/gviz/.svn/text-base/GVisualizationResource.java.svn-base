package org.wattdepot.resource.gviz;
//
//import org.restlet.Context;
//import org.restlet.data.MediaType;
//import org.restlet.data.Request;
//import org.restlet.data.Response;
//import org.restlet.resource.Representation;
import org.restlet.resource.Resource;
//import org.restlet.resource.ResourceException;
//import org.restlet.resource.StringRepresentation;
//import org.restlet.resource.Variant;
//
/**
 * Provides access to SensorData via the Google Visualization API using the Google Visualization
 * data source library. The data source library typically wraps its functionality as a servlet,
 * requiring a servlet container. Since WattDepot is based on Restlet, this class wraps the data
 * source library as a resource. <b>NOTE:</b> this class is currently unused, as we I decided that
 * it was easier to just go with the servlet method. I have preserved this class in case I decide to
 * try to restlet-ify the servlet later.
 * 
 * @see <a
 * href="http://code.google.com/apis/visualization/documentation/dev/implementing_data_source_overview.html">Google
 * Visualization API</a>
 * @author Robert Brewer
 */

public class GVisualizationResource extends Resource {
//
//  /** The parameters sent to the data source. */
//  protected String parameters = null;
//
//  /**
//   * Creates a new GVisualizationResource object with the provided parameters, and only an
//   * application/json representation.
//   * 
//   * @param context Restlet context for the resource
//   * @param request Restlet request
//   * @param response Restlet response
//   */
//  public GVisualizationResource(Context context, Request request, Response response) {
//    super(context, request, response);
//
//    // Contains anything after the GVIZ_URI
//    this.parameters = (String) request.getAttributes().get("parameters");
//
//    // This resource has only one type of representation.
//    // getVariants().add(new Variant(MediaType.APPLICATION_JSON));
//    getVariants().add(new Variant(MediaType.TEXT_PLAIN));
//  }
//
//  /**
//   * Returns a full representation for a given variant.
//   * 
//   * @param variant the requested variant of this representation
//   * @return the representation of this resource
//   * @throws ResourceException when the requested resource cannot be represented as requested.
//   */
//  @Override
//  public Representation represent(Variant variant) throws ResourceException {
//
//    // STOPPED HERE
//
//    // return new StringRepresentation("Foo", MediaType.APPLICATION_JSON);
//    if (this.parameters == null) {
//      return new StringRepresentation("No parameters", MediaType.TEXT_PLAIN);
//    }
//    else {
//      return new StringRepresentation(parameters, MediaType.TEXT_PLAIN);
//    }
//  }
//
}